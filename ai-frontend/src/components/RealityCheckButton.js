import React from 'react';

const RealityCheckButton = ({ onClick }) => {
  return (
    <button
      className="reality-check-btn"
      onClick={onClick}
    >
      Reality Check
    </button>
  );
};

export default RealityCheckButton;
